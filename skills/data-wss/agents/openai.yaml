interface:
  display_name: "AVE Data WebSocket"
  short_description: "Stream live prices, swaps, and klines with one-connection-first discipline"
  default_prompt: "Use $ave-data-wss to monitor live AVE market data, prefer one reusable connection with subscribe/unsubscribe, and summarize streams for chat surfaces."

policy:
  allow_implicit_invocation: true
